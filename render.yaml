services:
  - type: web
    name: nitan-mcp-server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js --transport=http --port=${PORT:-8000}
    plan: free
    autoDeploy: false
    envVars:
      - key: ENVIRONMENT
        value: production
